SimpleSQLiteExampleInUnity
===========

This is a Simple SQLite Example in Unity3d engine.

This Poject is configed to work properly with git(it includes a .gitignore file and also settings of unity for git).
